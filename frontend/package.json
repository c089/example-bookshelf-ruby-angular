{
  "name": "lunarlogic-bookshelf-frontend",
  "version": "0.0.0",
  "description": "bookshelf sample application",
  "scripts": {
    "postinstall": "bower install",
    "test": "jshint app/*.js && node_modules/karma/bin/karma start karma.conf.js --single-run"
  },
  "dependencies": {
    "bower": "^1.3.4",
    "karma": "^0.12.16",
    "karma-chrome-launcher": "^0.1.4"
  },
  "devDependencies": {
    "jshint": "^2.5.1",
    "karma-mocha": "^0.1.3",
    "mocha": "^1.20.1"
  }
}
